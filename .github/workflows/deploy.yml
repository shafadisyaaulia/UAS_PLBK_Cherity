name: Deploy AI-Safe Virtual Lab

# Disabled: Vercel handles deployment automatically
# This workflow is kept for reference only
on:
  workflow_dispatch:  # Manual trigger only

jobs:
  # Vercel auto-deploys on push to main
  # No need for separate GitHub Actions workflow
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "✅ Deployment handled by Vercel automatically"
          echo "📦 Frontend: Auto-deployed from GitHub"
          echo "🐍 Backend: Serverless functions in /api"
          echo "🚀 No manual deployment needed!"
